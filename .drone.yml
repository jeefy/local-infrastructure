pipeline:
  apply:
    privileged: true
    volumes:
      - /home/jeefy/.kube/config:/root/.kube/config
    image: lachlanevenson/k8s-kubectl:v1.9.2
    commands:
      - kubectl get pods
      - pwd
      - ls
      - ./apply.sh
